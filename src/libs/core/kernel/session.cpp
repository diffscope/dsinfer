#include "session.h"

namespace dsinfer {

    Session::Session() {
    }

    Session::~Session() {
    }

    bool Session::load(const std::filesystem::path &path, std::string *errorMessage) {
        return false;
    }

}